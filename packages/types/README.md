## @skillforge/types

Reserved for shared types between `apps/api` and `apps/web`.

In later phases we can:

- generate API client + types from OpenAPI
- or create a shared `zod` schema package for request/response validation

